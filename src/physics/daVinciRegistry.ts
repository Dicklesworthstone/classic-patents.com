import { PATENT_PHYSICS_REGISTRY } from "./telemetryData";

/** Compatibility export for older imports; the canonical entry lives in the catalogue registry. */
export const daVinciRegistryEntry = PATENT_PHYSICS_REGISTRY["us-6331181-davinci"];
