/**
 * Compatibility names for the retired Diesel edition module.
 *
 * The active candidate is dieselEngineEdition.ts. The catalogue remains
 * detached while the foreign-patent line and figure semantics are reviewed;
 * this wrapper prevents the old, unsupported editorial copy from resurfacing.
 */
export {
  dieselEngineArchivalEdition as dieselArchivalEdition,
  dieselEngineArchivalEdition as dieselEdition,
  dieselEngineParallelReadings as dieselParallelReadings,
} from "./dieselEngineEdition";
